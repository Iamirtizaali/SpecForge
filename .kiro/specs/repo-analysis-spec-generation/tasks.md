Now generate tasks.md based on the requirements and design. Break the work into discrete, trackable tasks that 3 developers can work on in parallel over ~6 hours.

Organize tasks into:
- Setup tasks (project scaffolding, env vars, dependencies)
- Frontend tasks (UI components, pages, styling)
- Backend tasks (API routes, GitHub integration, Bedrock integration, spec generation)
- Integration tasks (wiring frontend to backend, end-to-end testing)
- Polish tasks (error states, loading states, edge cases)
- Documentation tasks (README, dev logs, demo prep)

Each task should:
- Have a checkbox: `- [ ]`
- Be completable in 30-90 minutes
- Reference which requirement it implements (e.g., "implements 1.3")
- Indicate dependencies (e.g., "depends on: Setup-1")

Group by parallel work streams so 3 developers can work without blocking each other.